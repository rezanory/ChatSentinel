{
  "name": "chatsentinel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first watchdog and recovery controller for ChatGPT project work",
  "scripts": {
    "start": "node src/local-watchdog.js",
    "test": "node --test",
    "check": "node --check src/recovery-engine.js && node --check src/project-reconciler.js && node --check src/heartbeat.js && node --check src/local-watchdog.js && node --check extension/content.js && node --check extension/background.js"
  },
  "engines": {
    "node": ">=20"
  }
}
